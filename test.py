a = """title TEXT,
description TEXT,
collection TEXT,
reference TEXT,
version REAL,
year INTEGER,
instances INTEGER,
missing INTEGER,
variables INTEGER,
source TEXT,
url TEXT,
custom TEXT,
domain_name TEXT,"""

print(a.split("\n"))
for i in a.split("\n"):
    pass